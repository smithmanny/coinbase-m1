import { makeStyles } from 'integrations/material-ui'

export default makeStyles((theme) => ({
  startTransferButton: {
    padding: theme.spacing(2, 3)
  },
  startTransferButtonContainer: {
      textAlign: 'center',
      marginBottom: theme.spacing(8),
      marginTop: theme.spacing(8),
    },
  })
);
